#pragma once

#include "Texture/Texture.h"
#include <vector>

// Reference : Search " texture prefetching " on Google.

namespace gr
{
    class TextureManager
    {
    private:
        int numTextureLimit { 0 };
    };
};